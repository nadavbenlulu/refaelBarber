<!doctype html>
<html lang="he" dir="rtl">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <title>Refael Cohen | הזמנת תור</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <style>
      :root {
        --barber-black: #1a1a1a; 
        --barber-gold: #c5a059;
      }

      body {
        background-color: #f4f4f4; /* רקע בהיר כמו בתמונה */
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
      }

      /* הפס השחור למעלה */
      .top-header {
        background-color: var(--barber-black);
        color: white;
        padding: 15px 0;
        text-align: center;
        border-bottom: 3px solid var(--barber-gold);
      }

      .top-header h2 {
        margin: 0;
        font-size: 1.5rem;
        font-weight: bold;
        letter-spacing: 1px;
      }

      /* מיכל הטופס הלבן */
      .main-container {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 80vh;
        padding: 20px;
      }

      .form-card {
        background: white;
        padding: 40px;
        border-radius: 20px;
        box-shadow: 0 4px 25px rgba(0,0,0,0.1);
        width: 100%;
        max-width: 800px;
      }

      /* כפתור הוואטסאפ הירוק בצד */
      #whatsapp-bubble {
        position: fixed;
        bottom: 30px;
        right: 30px; /* צד ימין לפי התמונה */
        width: 60px;
        height: 60px;
        background-color: #25d366;
        border-radius: 50%;
        box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 1000;
        transition: transform 0.3s;
      }

      #whatsapp-bubble:hover {
        transform: scale(1.1);
      }

      .text-gold {
        color: var(--barber-gold) !important;
      }
    </style>
  </head>
  <body>

  <header class="top-header">
      <div class="container">
          <h2>REFAEL COHEN <i class="bi bi-scissors text-gold"></i></h2>
      </div>
  </header>

  <main class="main-container">
      <div class="form-card">
          {{{body}}}
      </div>
  </main>

  <a href="https://wa.me/972506764652" target="_blank" id="whatsapp-bubble">
      <i class="bi bi-whatsapp text-white fs-1"></i>
  </a>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>